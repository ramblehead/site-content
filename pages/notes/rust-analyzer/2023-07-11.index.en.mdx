---
title: Rust and Rust Analyzer Notes
dateUpdated: '2023-07-11'
description:
  Collection of Rust and rust-analyzer application hints and resource
  references. These hints were tested on Ubuntu 22.04 LTS.
---

## Installing Rust on Linux

### Automatic Installation via Rustup

To install rust toolchain:

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

To add rust source (required for rust-analyzer):

```bash
rustup component add rust-src
```

To update rust toolchain:

```bash
rustup update
```

To uninstall rust toolchain:

```bash
rustup self uninstall
```

### Environment Configuration

Add the following lines to `~/.bashrc` (for interactive shells) and to
`/.profile` (for login shells):

```bash
. "$HOME/.cargo/env"
```

## Installing Rust Analyzer

### From Source Code

To clone and compile rust-analyzer from source:

```bash
git clone https://github.com/rust-analyzer/rust-analyzer.git -b release
cd rust-analyzer
cargo xtask install --server # will install rust-analyzer into $HOME/.cargo/bin
```

### From Pre-compiled Binary

Download and unpack the latest [rust-analyzer
release](https://github.com/rust-lang/rust-analyzer/releases) e.g.:

```bash
gunzip -vc rust-analyzer-x86_64-unknown-linux-gnu.gz > ~/.cargo/bin/rust-analyzer
```

### References

- [Configuring Emacs for Rust development](https://robert.kra.hn/posts/rust-emacs-setup/)
- [Notes about Rust installation](https://www.rust-lang.org/tools/install)
